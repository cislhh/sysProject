1，首页banner：轮播图正常播放没有问题，如果切换下面的二级路由后再回来，图片盒子会被扩大两倍，只有设定：：width才可以固定，但是只能用px单位，如果换了其他屏幕大小，轮播图的显示就会出现问题
2，商品分类sort：如果用axios引入的数据列表无法再当前页面进行刷新
3，首页最下面的footer：在切换的时候，依然无法让它一直保留在页面上，总是页面渲染完成后，这个组件才出来
4，所有需要渲染数据的页面都存在2的问题，如果在当前.vue中直接写接口，页面数据在刷新后不会丢失，如果是用vuex中拿到的state数组，页面刷新后就会state的数据就会消失，只有切换路由才会出现数据
5，comMsg页面有不明原因的报错Error in render: "TypeError: Cannot read property 'goodsname' of undefined"，但是报错不影响页面的
      已解决.....错误点：当前数据还没有加载到就急着渲染,可以先做一个判断v-if,得到这个数据了在渲染就可以了。
6，购物车页面的单选多选逻辑上有问题，因为没有checked，我在数据获取后添加，但是计算属性没有获取到里面的checked值，得不到正确的数据
      已解决......错误点：应该在res获取到的数组进行checked赋值，而不是在将res中list赋值给goodsList后再去赋值